import { SomthingDirective } from './somthing.directive';

describe('SomthingDirective', () => {
  it('should create an instance', () => {
    const directive = new SomthingDirective();
    expect(directive).toBeTruthy();
  });
});
